# === Traefik-Exposed Hostnames (examples) ===
TRAEFIK_WEB=traefik.example.com
PIHOLE_WEB=pihole.example.com
PROMETHEUS_WEB=prometheus.example.com
GRAFANA_WEB=grafana.example.com
WEBSITE_WEB=example.com
PORTAINER_WEB=portainer.example.com

# === Cloudflare DNS API for Let's Encrypt ===
# Create a scoped token in Cloudflare (Zone:DNS Edit, Zone:Read)
CLOUDFLARE_DNS_API_TOKEN=CHANGEME

# === Pi-hole ===
WEBPASSWORD=CHANGEME

# === Grafana Admin Credentials ===
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=CHANGEME

# === Traefik Basic Auth (user:hashed_pass) ===
# Generate: htpasswd -nbB mirage 'YourPassword' | sed -e 's/\$/\$\$/g'
TRAEFIK_AUTH=mirage:REPLACE_WITH_BCRYPT_HASH

